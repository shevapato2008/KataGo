# KataGo Real-Time API Configuration

# Path to the compiled KataGo executable
KATAGO_PATH=./cpp/katago

# Path to the KataGo analysis configuration file
KATAGO_CONFIG_PATH=./cpp/configs/analysis_example.cfg

# Path to the neural network model file (.bin.gz)
KATAGO_MODEL_PATH=./model.bin.gz

# Optional: Host and Port for the API server (used if running via script)
HOST=0.0.0.0
PORT=8000
